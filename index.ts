import * as R from "./router/RouterProvider";

const RouterProvider = R

export default RouterProvider

export const BrowserRouterProvider = R.BrowserRouterProvider

export const HashRouterProvider = R.HashRouterProvider